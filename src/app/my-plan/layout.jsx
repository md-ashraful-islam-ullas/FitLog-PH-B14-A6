import React from "react";

export const metadata = {
  title: "My Plan | FitLog",
  description: "Manage your workout plan and saved exercises.",
};

const MyPlanLayout = ({ children }) => {
  return <>{children}</>;
};

export default MyPlanLayout;